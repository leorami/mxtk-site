'use client';import { useEffect, useState } from 'react';import Button from './ui/Button';export default function CookieConsent(){const [visible,setVisible]=useState(false);useEffect(()=>{if(!localStorage.getItem('cookie-consent'))setVisible(true);},[]);if(!visible)return null;return(<div className='fixed bottom-4 left-1/2 z-50 w-[95%] max-w-3xl -translate-x-1/2 rounded-2xl border border-border bg-surface p-4 shadow-lg dark:bg-dark-surface'><p className='text-sm text-muted'>We use privacy-friendly analytics. No tracking until you consent.</p><div className='mt-3 flex gap-3'><Button onClick={()=>{localStorage.setItem('cookie-consent','accepted');setVisible(false);}}>Accept</Button><button className='text-sm underline' onClick={()=>{localStorage.setItem('cookie-consent','declined');setVisible(false);}}>Decline</button></div></div>);}