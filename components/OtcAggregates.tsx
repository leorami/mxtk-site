import Card from '@/components/ui/Card';type Row={month:string;trades:number;notionalUsd:string;notes?:string};export default function OtcAggregates({rows}:{rows:Row[]}){return(<Card><div className='text-sm font-semibold'>OTC monthly aggregates (anonymized)</div><div className='mt-2 overflow-x-auto'><table className='table text-sm'><thead><tr><th>Month</th><th>Trades</th><th>Notional (USD)</th><th>Notes</th></tr></thead><tbody>{rows.map((r,i)=>(<tr key={i}><td>{r.month}</td><td>{r.trades}</td><td>{r.notionalUsd}</td><td className='text-muted'>{r.notes??'—'}</td></tr>))}</tbody></table></div><p className='mt-2 text-xs text-muted'>Aggregates exclude personally identifiable info and counterparties.</p></Card>);}