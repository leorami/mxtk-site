# MXTK Site - Environment Template
# Copy this file to `.env` (or the appropriate env file for your runtime) and fill in values.
# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser.
# - Leave unset to use defaults shown in code where applicable.

# =====================
# Admin / Access Tokens (Required for /admin)
# =====================
# Admin login token used at /admin. Share privately with your team.
MXTK_ADMIN_TOKEN=
# Server-side admin token for protected admin APIs (Bearer). Use the same value as above.
ADMIN_TOKEN=
# Public counterpart so admin UI fetches can include the same Bearer token when needed.
NEXT_PUBLIC_ADMIN_TOKEN=

# =====================
# Core Site (Public)
# =====================
# Base path when served behind a subpath (e.g., /mxtk). Leave empty for root.
NEXT_PUBLIC_BASE_PATH=
# Canonical site origin used by robots/sitemap and security checks.
NEXT_PUBLIC_SITE_ORIGIN=
# Optional: Toggle dev AI pages/components in the UI (1 to enable).
NEXT_PUBLIC_ENABLE_AI_DEV=0

# =====================
# Build/Runtime (Next.js)
# =====================
# Optional custom Next.js dist directory. Leave unset to use default `.next`.
NEXT_DIST_DIR=

# =====================
# AI / Sherpa (Models, Providers, Keys)
# =====================
# Provider: 'openai' or 'openrouter'
AI_PROVIDER=openai

# OpenAI API key (preferred). If absent, the app also checks `openai_api_key`.
OPENAI_API_KEY=
# Lowercase alias supported by the app (use only if needed).
openai_api_key=

# OpenRouter (optional, if AI_PROVIDER=openrouter)
OPENROUTER_API_KEY=
OPENROUTER_SITE=https://mxtk

# Model selection (comma-separated lists)
AI_MODEL=gpt-4o
AI_MODEL_TIER_SUGGEST=gpt-4o-mini
AI_MODEL_TIER_ANSWER=gpt-4o
AI_MODEL_TIER_DEEP=gpt-4.1
AI_EMBED_MODEL=text-embedding-3-large

# =====================
# AI Vector Store & Costs
# =====================
# Directory for vector store and AI logs (costs, flags, journeys, etc.)
AI_VECTOR_DIR=./ai_store

# Soft daily cost budget (USD). 0 disables budget.
COST_BUDGET_DAILY_USD=0
# Behavior when budget is reached: 'warn' | 'degrade' | 'block'
COST_BUDGET_MODE=degrade

# =====================
# Admin / Auth
# =====================
# Server-side admin token for protected routes.
ADMIN_TOKEN=dev
# Admin login token for session API; must match what admins enter in UI.
MXTK_ADMIN_TOKEN=
# Frontend header/admin tools (public, e.g., used by admin UI fetches)
NEXT_PUBLIC_ADMIN_TOKEN=dev

# =====================
# On-chain / Market Data
# =====================
# Required: Arbitrum RPC URL (e.g., from a provider like Alchemy/Infura)
ARBITRUM_RPC_URL=

# MXTK token configuration
MXTK_TOKEN_ADDRESS=0x3e4Ffeb394B371AAaa0998488046Ca19d870d9Ba
# Optional manual seed list of pools (comma-separated addresses)
MXTK_POOLS=

# Auto-discovery controls
AUTO_DISCOVER_POOLS=1
# Comma-separated fee tiers, e.g., 100,500,3000
DISCOVERY_FEES=
# Comma-separated override list of stable token addresses
DISCOVERY_QUOTES=

# Optional indexer fallback
DEXSCREENER_BASE=https://api.dexscreener.com/latest/dex

# Optional Uniswap subgraph endpoint (Arbitrum)
UNISWAP_V4_SUBGRAPH_URL_ARBITRUM=

# Stable coin symbols list (comma-separated)
STABLE_SYMBOLS=USDC,USDC.e,USDT,DAI

# Cache TTL for certain lookups (seconds)
CACHE_TTL_SECONDS=45

# =====================
# Local Dev / Tests / Tools
# =====================
# Base URL used by test runner and tooling (dev server or ngrok URL)
BASE_URL=http://localhost:2000

# Optional: Dev tunnel (ngrok) integration used by scripts
NGROK_STATIC_DOMAIN=
NGROK_DOMAIN=
